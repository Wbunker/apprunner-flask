version: 1.0
runtime: python3
build:
  commands:
    build:
    - python -m pip install -r requirements.txt
run:
  runtime-version: 3.8
  command: python -m flask run --host=0.0.0.0 --port=8080
  network:
    port: 8080
